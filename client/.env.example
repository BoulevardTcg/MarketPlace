# Phase 2 : chemins relatifs via reverse proxy (Caddy :3000 ou Vite proxy).
# Ne plus utiliser de base URLs absolues.
# VITE_API_URL=/market       (défaut si variable absente)
# VITE_BOUTIQUE_API_URL=/api (défaut si variable absente)
#
# Fallback pour dev SANS proxy (déconseillé) :
# VITE_API_URL=http://localhost:8081
# VITE_BOUTIQUE_API_URL=http://localhost:8080/api
