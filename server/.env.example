# Environment
NODE_ENV=development
PORT=8081

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/boulevard_market

# JWT (auth gérée par le projet Shop)
# RS256: clé publique pour vérifier les tokens signés par Shop
JWT_PUBLIC_KEY=
# Ou HS256: secret partagé (si pas de RS256)
JWT_SECRET=

# CORS (optionnel — en dev, défaut http://localhost:5173 pour le client Vite)
# CORS_ORIGIN=http://localhost:5173

# Listing images (presigned S3 upload, optionnel — 503 si absent)
# LISTING_IMAGES_BUCKET=my-bucket
# AWS_REGION=eu-west-1
