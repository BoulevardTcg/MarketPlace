# Environment
NODE_ENV=development
PORT=8081

# Database (PostgreSQL via docker compose up -d postgres)
DATABASE_URL=postgresql://boulevard:boulevard_dev@localhost:5432/boulevard_market

# JWT (auth gérée par la Boutique / server)
# ⚠️ NE JAMAIS mettre JWT_PRIVATE_KEY ici (Marketplace vérifie uniquement avec la clé publique).
# RS256 (recommandé) — option A : clé publique en variable (copier JWT_PUBLIC_KEY de la Boutique)
JWT_PUBLIC_KEY=
# RS256 — option B (plus propre) : fichier PEM monté, pas de PEM dans l'env
# JWT_PUBLIC_KEY_PATH=/app/keys/jwt_public.pem
# HS256 (legacy) : si la Boutique n’utilise pas RS256, mettre le même JWT_SECRET que server/.env
JWT_SECRET=

# CORS (optionnel — en dev, défaut 5173 + 5174 pour Boutique et Marketplace)
# CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Listing images (presigned S3 upload, optionnel — 503 si absent)
# LISTING_IMAGES_BUCKET=my-bucket
# AWS_REGION=eu-west-1
